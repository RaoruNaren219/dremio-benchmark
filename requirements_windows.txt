pyspark==3.2.1
pandas==1.3.5
matplotlib==3.5.3
seaborn==0.11.2
requests==2.28.1
numpy==1.21.6
pyyaml==6.0
pytest==7.2.0
pytest-cov==4.0.0
python-dotenv==0.21.0
pyarrow==10.0.1
joblib==1.2.0
# Additional dependencies for Python-native HDFS operations
hdfs==2.7.0

# Note: Kerberos-related packages that cause build issues on Windows are commented out
# kerberos==1.3.1
# gssapi==1.8.2
# pydoop==2.0.0

# Alternative that works better on Windows
requests-kerberos==0.14.0

# Note: For Kerberos authentication on Windows, you may need to:
# 1. Install MIT Kerberos for Windows: https://web.mit.edu/kerberos/dist/
# 2. Set up the correct environment variables
# 3. Or use WSL for Kerberos operations 